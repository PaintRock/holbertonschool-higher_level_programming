#!/usr/bin/node

class Rectangle {
  if (w <= 0 || h <= || typeof w !== 'number' || typeof h!== 'number')
  constructor (w, h) {
    this.width = w;
    this.height = h;
  }
}
module.exports = Rectangle;
